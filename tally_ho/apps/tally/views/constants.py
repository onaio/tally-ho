# query params - would be best if values for this in the url,
# be a str (<enum>.name when dealing with enums) and in small case.
form_state_query_param = 'form_state'
race_type_query_param = 'race_type'
